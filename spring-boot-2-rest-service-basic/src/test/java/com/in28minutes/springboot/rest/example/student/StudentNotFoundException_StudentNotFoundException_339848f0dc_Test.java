// Test generated by RoostGPT for test RestBasicService using AI Type Open AI and AI Model gpt-4

package com.in28minutes.springboot.rest.example.student;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class StudentNotFoundException_StudentNotFoundException_339848f0dc_Test {

    @Mock
    private StudentNotFoundException studentNotFoundException;

    @InjectMocks
    private StudentService studentService;

    @BeforeEach
    public void setup() {
        MockitoAnnotations.initMocks(this);
    }

    @Test
    public void testExceptionMessage() {
        String exceptionMessage = "Student not found";
        try {
            throw new StudentNotFoundException(exceptionMessage);
        } catch (StudentNotFoundException e) {
            assertEquals(exceptionMessage, e.getMessage());
        }
    }

    @Test
    public void testExceptionMessageWithDifferentMessage() {
        String exceptionMessage = "Different exception message";
        try {
            throw new StudentNotFoundException(exceptionMessage);
        } catch (StudentNotFoundException e) {
            assertEquals(exceptionMessage, e.getMessage());
        }
    }
}
