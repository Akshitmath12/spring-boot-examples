// Test generated by RoostGPT for test RestBasicService using AI Type Open AI and AI Model gpt-4

package com.in28minutes.springboot.rest.example.student;

import static org.junit.jupiter.api.Assertions.assertEquals;
import static org.mockito.Mockito.when;
import static org.mockito.Mockito.anyString;

import org.junit.jupiter.api.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
public class Student_setPassportNumber_27b9a7834a_Test {

    @InjectMocks
    private Student student;

    @Mock
    private ExternalService externalService;

    @Test
    public void testSetPassportNumber_ValidNumber() {
        String validPassportNumber = "123456789";
        when(externalService.validatePassportNumber(anyString())).thenReturn(true);
        student.setPassportNumber(validPassportNumber);
        assertEquals(validPassportNumber, student.getPassportNumber());
    }

    @Test
    public void testSetPassportNumber_InvalidNumber() {
        String invalidPassportNumber = "123";
        when(externalService.validatePassportNumber(anyString())).thenReturn(false);
        student.setPassportNumber(invalidPassportNumber);
        assertEquals(null, student.getPassportNumber());
    }
}
